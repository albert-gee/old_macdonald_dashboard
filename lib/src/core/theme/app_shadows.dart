import 'package:flutter/material.dart';

class AppShadows {
  static final List<BoxShadow> card = [
    BoxShadow(
      color: const Color(0xFF000000).withValues(alpha: 77),
      blurRadius: 10,
      offset: Offset(0, 4),
    ),
  ];
}
